name: CI

on:
  push:
    branches: [main, master]
  pull_request:

jobs:
  build-test:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        node-version: ["22", "24"]
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-node@v7
        with:
          node-version: ${{ matrix.node-version }}
          cache: npm

      - run: npm ci

      # Check that production dependencies have no high-severity vulnerabilities.
      - name: Audit (prod deps)
        run: npm audit --omit=dev --audit-level=high

      - name: Typecheck
        run: npm run typecheck

      - name: Lint
        run: npm run lint

      # Enforce English-only source (no CJK in src/test).
      - name: Lint language
        run: npm run lint:lang

      # Prettier owns formatting outright -- eslint-config-prettier turns off ESLint's formatting
      # rules -- so without this step nothing in the pipeline would reject a badly formatted file.
      - name: Format check
        run: npm run format:check

      - name: Build
        run: npm run build

      # Generate the Prisma client so the Prisma adapter e2e suite runs (it self-skips otherwise),
      # which is required for prisma.ts to meet the coverage threshold.
      - name: Generate Prisma client
        run: npm run test:prisma:gen

      # The ubuntu runner ships with Docker, so testcontainers integration tests can run too.
      - name: Test (with coverage)
        run: npm run test:coverage

      # Validate that the published package's exports / type resolution are correct for both ESM and CJS.
      - name: Validate package publish surface
        run: npm run lint:publish

      # Verify the public d.ts surface matches etc/commitcourier.api.md (breaking-change detection).
      - name: Verify public API surface
        run: npm run api:check

  # Verify the supported PostgreSQL range. The build-test job already runs the full coverage
  # suite against the default image; this job re-runs only the integration suite across the
  # minimum and latest supported majors so the README's "PostgreSQL 12+" claim is actually
  # exercised, without multiplying the coverage matrix.
  integration-postgres:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        postgres: ["postgres:12-alpine", "postgres:16-alpine", "postgres:17-alpine"]
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Integration tests (PostgreSQL ${{ matrix.postgres }})
        run: npm run test:integration
        env:
          POSTGRES_IMAGE: ${{ matrix.postgres }}

  # Pack the tarball and import it from a clean, separate project (ESM + CJS) so a broken
  # published surface — missing files, wrong exports, an optional peer turned mandatory — fails
  # CI rather than the first consumer's install.
  pack-smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - run: npm run build
      - name: Pack & install smoke test
        run: npm run smoke:pack

  # Mutation testing is heavy, so run it as a separate job (core only).
  mutation:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7
      - uses: actions/setup-node@v7
        with:
          node-version: "22"
          cache: npm
      - run: npm ci
      - name: Mutation testing (core only)
        run: npm run test:mutation
